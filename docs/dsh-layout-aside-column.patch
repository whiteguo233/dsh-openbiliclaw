diff -ru /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/AppFrame.module.css /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/AppFrame.module.css
--- /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/AppFrame.module.css	2026-08-11 01:10:34
+++ /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/AppFrame.module.css	2026-08-13 00:49:11
@@ -42,12 +42,23 @@
   border-left: 1px solid var(--dsw-alias-border-l2);
 }
 
-/* The details subtree stays mounted at zero width, so its border must not paint
-   a 1px seam. The collapsed sidebar instead retains a bordered compact rail. */
+.asideCol {
+  min-width: 0;
+  overflow: hidden;
+  border-left: 1px solid var(--dsw-alias-border-l1);
+}
+
+/* The details/aside subtrees stay mounted at zero width, so their borders must
+   not paint a 1px seam. The collapsed sidebar instead retains a bordered
+   compact rail. */
 .frame[data-details-collapsed] .detailsCol {
   border-left: none;
 }
 
+.frame[data-aside-collapsed] .asideCol {
+  border-left: none;
+}
+
 /* Drag handles are frame children (columns clip overflow): an 8px hit strip
    centered on the column border via inline left, above column content. Details
    adds a visible 12x32 pill at vertical center; sidebar keeps only the hit strip. */
@@ -105,4 +116,75 @@
 .handle[data-side='details'][data-dragging='true']::after {
   background: var(--dsw-alias-button-floating-hover);
   border-color: var(--dsw-alias-border-l3);
+}
+
+/* The aside handle mirrors the details pill affordance. */
+.handle[data-side='aside']::after {
+  content: '';
+  position: absolute;
+  top: 50%;
+  left: 50%;
+  transform: translate(-50%, -50%);
+  width: 12px;
+  height: 32px;
+  border-radius: 10px;
+  box-sizing: border-box;
+  background: var(--dsw-alias-button-floating-fill);
+  border: 1px solid var(--dsw-alias-border-l2-darkmode-thin);
+  opacity: 0;
+  transition:
+    opacity var(--ds-transition-duration-slow) var(--ds-ease-in-out),
+    background var(--ds-transition-duration-slow) var(--ds-ease-in-out);
+}
+
+.asideCol:hover ~ .handle[data-side='aside']::after,
+.handle[data-side='aside']:hover::after,
+.handle[data-side='aside'][data-dragging='true']::after {
+  opacity: 1;
+}
+
+.handle[data-side='aside']:hover::after,
+.handle[data-side='aside'][data-dragging='true']::after {
+  background: var(--dsw-alias-button-floating-hover);
+  border-color: var(--dsw-alias-border-l3);
+}
+
+/* The closed aside's reopen affordance: a slim full-height rail at the frame
+   edge with a vertical label and an inward chevron. */
+.asideRail {
+  position: absolute;
+  top: 0;
+  bottom: 0;
+  right: 0;
+  width: 28px;
+  border: none;
+  border-left: 1px solid var(--dsw-alias-border-l1);
+  background: var(--dsw-alias-bg-layer-1);
+  color: var(--dsw-alias-label-secondary);
+  cursor: pointer;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  justify-content: center;
+  gap: 4px;
+  padding: 0;
+  z-index: 2;
+  transition: background var(--ds-transition-duration-slow) var(--ds-ease-in-out), color var(--ds-transition-duration-slow) var(--ds-ease-in-out);
+}
+
+.asideRail:hover {
+  background: var(--dsw-alias-interactive-bg-hover);
+  color: var(--dsw-alias-label-primary);
+}
+
+.asideRailChevron {
+  font-size: 14px;
+  line-height: 1;
+}
+
+.asideRailLabel {
+  writing-mode: vertical-rl;
+  font-size: 10px;
+  font-weight: 600;
+  letter-spacing: 0.12em;
 }
diff -ru /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/AppFrame.tsx /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/AppFrame.tsx
--- /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/AppFrame.tsx	2026-08-11 01:10:34
+++ /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/AppFrame.tsx	2026-08-13 00:52:14
@@ -20,7 +20,7 @@
 /** Full composed props: runtime share + child-slot render share + store share. */
 export type AppFrameProps =
   & PropsRuntime<'root'>
-  & PropsRenderSlots<'sidebar' | 'conversation' | 'details'>
+  & PropsRenderSlots<'sidebar' | 'conversation' | 'details' | 'aside'>
   & PropsStore<ReturnType<typeof createLayoutStore>>
 
 /** Center column grid item (session-body building block). */
@@ -33,11 +33,16 @@
   return <div className={css.detailsCol}>{props.children}</div>
 }
 
+/** Aside column grid item; width 0 keeps the subtree mounted (never unmount on close). */
+function AsideColumn(props: { children?: ReactNode }) {
+  return <div className={css.asideCol}>{props.children}</div>
+}
+
 /**
  * One drag handle: pointer capture, rAF-throttled dx reports against the drag-start origin.
  * `side` keys the hover-reveal CSS to the owning column.
  */
-function DragHandle(props: { side: 'sidebar' | 'details'; left: number; onStart: () => void; onDrag: (dx: number) => void; onEnd: () => void }) {
+function DragHandle(props: { side: 'sidebar' | 'details' | 'aside'; left: number; onStart: () => void; onDrag: (dx: number) => void; onEnd: () => void }) {
   const [dragging, setDragging] = useState(false)
   const origin = useRef(0)
   const latest = useRef(0)
@@ -139,7 +144,7 @@
   const sidebarPreference = sidebarCollapsed
     ? 0
     : panels.sidebar === 0 ? SIDEBAR_DEFAULT : panels.sidebar
-  const cols = computeColumns(viewport, sidebarPreference, detailsSession === undefined ? 0 : panels.details)
+  const cols = computeColumns(viewport, sidebarPreference, detailsSession === undefined ? 0 : panels.details, panels.aside)
   const colsRef = useRef(cols)
   colsRef.current = cols
 
@@ -148,26 +153,34 @@
   // it stays frozen for the whole gesture so dx deltas do not compound.
   const sidebarBase = useRef(0)
   const detailsBase = useRef(0)
+  const asideBase = useRef(0)
   // Track-level transitions pause for the whole gesture: eased tracks would
   // detach the column edge from the pointer (AppFrame.module.css).
   const [dragging, setDragging] = useState(false)
   const onDragEnd = useCallback(() => { setDragging(false) }, [])
   const onSidebarStart = useCallback(() => { sidebarBase.current = colsRef.current.sidebar; setDragging(true) }, [])
   const onDetailsStart = useCallback(() => { detailsBase.current = colsRef.current.details; setDragging(true) }, [])
+  const onAsideStart = useCallback(() => { asideBase.current = colsRef.current.aside; setDragging(true) }, [])
   const onSidebarDrag = useCallback((dx: number) => {
     actions.setSidebar(sidebarBase.current + dx)
   }, [actions])
   const onDetailsDrag = useCallback((dx: number) => {
     actions.setDetails(detailsBase.current - dx)
   }, [actions])
+  const onAsideDrag = useCallback((dx: number) => {
+    // The handle straddles the aside's LEFT edge (between details and aside):
+    // dragging right moves the boundary right and NARROWS the column.
+    actions.setAside(asideBase.current - dx)
+  }, [actions])
 
   return (
     <div
       ref={frameRef}
       className={css.frame}
-      style={{ gridTemplateColumns: `${cols.sidebar}px minmax(0, 1fr) ${cols.details}px` }}
+      style={{ gridTemplateColumns: `${cols.sidebar}px minmax(0, 1fr) ${cols.details}px ${cols.aside}px` }}
       data-sidebar-collapsed={sidebarCollapsed || undefined}
       data-details-collapsed={cols.details === 0 || undefined}
+      data-aside-collapsed={cols.aside === 0 || undefined}
       data-dragging={dragging || undefined}
     >
       <div className={css.sidebarCol}>
@@ -186,13 +199,27 @@
             paint — no loading gate: a bare status line reads worse than
             the shell's own pending rendering. The conversation
             is session-maybe; the strict details entry naturally renders
-            empty while no session is current. */}
+            empty while no session is current. The aside occupant is
+            root-scoped and renders unconditionally (like the sidebar). */}
         <CenterColumn>{renderSlot('conversation', {})}</CenterColumn>
         <DetailsColumn>{renderSlot('details', {})}</DetailsColumn>
+        <AsideColumn>{renderSlot('aside', {
+          collapsed: cols.aside === 0,
+          width: cols.aside,
+        })}</AsideColumn>
       </>
       {/* The collapsed rail is fixed-width: no resize handle while closed. */}
       {!sidebarCollapsed && <DragHandle side="sidebar" left={cols.sidebar} onStart={onSidebarStart} onDrag={onSidebarDrag} onEnd={onDragEnd} />}
       {cols.details > 0 && <DragHandle side="details" left={viewport - cols.details} onStart={onDetailsStart} onDrag={onDetailsDrag} onEnd={onDragEnd} />}
+      {cols.aside > 0 && <DragHandle side="aside" left={viewport - cols.aside} onStart={onAsideStart} onDrag={onAsideDrag} onEnd={onDragEnd} />}
+      {/* Closed aside keeps a slim reopen rail at the frame's right edge (the
+          column itself is zero-width, so the affordance lives at frame level). */}
+      {cols.aside === 0 ? (
+        <button type="button" className={css.asideRail} title="打开侧栏" onClick={() => actions.openAside()}>
+          <span className={css.asideRailChevron}>‹</span>
+          <span className={css.asideRailLabel}>侧栏</span>
+        </button>
+      ) : null}
     </div>
   )
 }
diff -ru /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/columns.ts /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/columns.ts
--- /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/columns.ts	2026-08-11 01:10:34
+++ /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/columns.ts	2026-08-12 21:52:03
@@ -1,22 +1,23 @@
 /**
- * Pure concession-chain column solver for the three-column AppFrame.
+ * Pure concession-chain column solver for the four-column AppFrame.
  * Chain order is fixed by contract: keep center >= CENTER_MIN by shrinking
- * details, then auto-closing it (derived zero width — preferred width
+ * the auxiliary aside column first, then auto-closing it, then shrinking
+ * details, then auto-closing details (derived zero widths — preferred width
  * preferences are never rewritten, so widening the window restores them).
  * The sidebar never concedes: its rendered width is always the drag
  * preference (or the collapsed rail), and center absorbs any remaining
  * deficit as the last resort. Inputs are the layout store's plain width
  * preferences (0 = closed); a closed sidebar resolves to the fixed
- * SIDEBAR_COLLAPSED control rail while closed details resolve to zero width.
- * The SIDEBAR_AUTO_COLLAPSE breakpoint is consumed by AppFrame, which decides
- * the effective sidebar preference before solving; the solver itself stays
- * breakpoint-free.
+ * SIDEBAR_COLLAPSED control rail while closed details/aside resolve to zero
+ * width. The SIDEBAR_AUTO_COLLAPSE breakpoint is consumed by AppFrame, which
+ * decides the effective sidebar preference before solving; the solver itself
+ * stays breakpoint-free.
  */
 
 /** Resolved widths for one frame; center may drop below CENTER_MIN only at the final fallback. */
-export interface Columns { sidebar: number; center: number; details: number }
+export interface Columns { sidebar: number; center: number; details: number; aside: number }
 
-// Contract-frozen geometry: the three-column concession chain's fixed points.
+// Contract-frozen geometry: the four-column concession chain's fixed points.
 /** Center column floor; only the final fallback may go below it. */
 export const CENTER_MIN = 640
 /** Sidebar drag clamp floor. */
@@ -37,6 +38,12 @@
 export const DETAILS_MAX = 520
 /** Details width before any user drag. */
 export const DETAILS_DEFAULT = 360
+/** Aside drag clamp floor. */
+export const ASIDE_MIN = 280
+/** Aside drag clamp ceiling. */
+export const ASIDE_MAX = 520
+/** Aside width before any user drag. */
+export const ASIDE_DEFAULT = 360
 
 /**
  * Clamp a panel width into its contract range.
@@ -50,28 +57,43 @@
 }
 
 /**
- * Solve the three column widths for one viewport frame. Pure: no hysteresis —
+ * Solve the four column widths for one viewport frame. Pure: no hysteresis —
  * the output is a function of (viewport, preferences) only, so recovery on
  * re-widening is automatic. Preferences re-clamp here because they cross the
  * store boundary and callers may still supply stale ranges.
  * @param viewport - available frame width in px.
  * @param sidebar - sidebar width preference in px (0 = closed).
  * @param details - details width preference in px (0 = closed).
- * @returns resolved widths; details 0 means visually closed (never unmounted), while a closed sidebar keeps its compact rail.
+ * @param aside - aside width preference in px (0 = closed).
+ * @returns resolved widths; details/aside 0 mean visually closed (never unmounted), while a closed sidebar keeps its compact rail.
  */
-export function computeColumns(viewport: number, sidebar: number, details: number): Columns {
+export function computeColumns(viewport: number, sidebar: number, details: number, aside: number): Columns {
   // The sidebar is fixed at its preference (or the rail) — it never concedes.
   const s = sidebar === 0 ? SIDEBAR_COLLAPSED : clampWidth(sidebar, SIDEBAR_MIN, SIDEBAR_MAX)
+  const a0 = aside === 0 ? 0 : clampWidth(aside, ASIDE_MIN, ASIDE_MAX)
   const d0 = details === 0 ? 0 : clampWidth(details, DETAILS_MIN, DETAILS_MAX)
 
   // Step 1: everything fits at preferred widths.
-  if (s + d0 + CENTER_MIN <= viewport) return { sidebar: s, center: viewport - s - d0, details: d0 }
+  if (s + a0 + d0 + CENTER_MIN <= viewport) {
+    return { sidebar: s, center: viewport - s - a0 - d0, details: d0, aside: a0 }
+  }
 
-  // Step 2: shrink details toward its minimum.
+  // Step 2: shrink aside toward its minimum (aside concedes before details).
+  const a1 = a0 === 0 ? 0 : Math.max(ASIDE_MIN, viewport - s - d0 - CENTER_MIN)
+  if (s + a1 + d0 + CENTER_MIN <= viewport) {
+    return { sidebar: s, center: CENTER_MIN, details: d0, aside: a1 }
+  }
+
+  // Step 3: auto-close aside (derived — preferences untouched).
+  if (s + d0 + CENTER_MIN <= viewport) {
+    return { sidebar: s, center: viewport - s - d0, details: d0, aside: 0 }
+  }
+
+  // Step 4: shrink details toward its minimum.
   const d1 = d0 === 0 ? 0 : Math.max(DETAILS_MIN, viewport - s - CENTER_MIN)
-  if (s + d1 + CENTER_MIN <= viewport) return { sidebar: s, center: CENTER_MIN, details: d1 }
+  if (s + d1 + CENTER_MIN <= viewport) return { sidebar: s, center: CENTER_MIN, details: d1, aside: 0 }
 
-  // Step 3: auto-close details (derived — preferences untouched); center
+  // Step 5: auto-close details (derived — preferences untouched); center
   // absorbs any remaining deficit (may drop below CENTER_MIN).
-  return { sidebar: s, center: Math.max(0, viewport - s), details: 0 }
+  return { sidebar: s, center: Math.max(0, viewport - s), details: 0, aside: 0 }
 }
diff -ru /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/index.ts /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/index.ts
--- /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/index.ts	2026-08-11 01:10:34
+++ /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/index.ts	2026-08-12 21:53:09
@@ -33,7 +33,7 @@
 declare module '@deepseek-ai/dsh-client-ui-slots' {
   interface SlotMap {
     // The 'root' entry itself is the runtime's built-in slot (declared
-    // there); these four are the frame's children, declared by the same
+    // there); these five are the frame's children, declared by the same
     // register() call that contributes AppFrame. Session owners never pass
     // sessionId: the framework injects it as a standard prop.
     'sidebar': { kind: 'single'; scope: 'root'; owner: SidebarOwnerProps }
@@ -41,6 +41,10 @@
     // and live conversation states without changing its React identity.
     'conversation': { kind: 'single'; scope: 'session-maybe'; owner: ConvOwnerProps }
     'details': { kind: 'single'; scope: 'session'; owner: DetailsOwnerProps }
+    // Auxiliary rightmost panel (root-scoped): a plugin occupies it to host
+    // a persistent side panel next to the conversation, independent of the
+    // current session (e.g. an external-service dashboard).
+    'aside': { kind: 'single'; scope: 'root'; owner: AsideOwnerProps }
   }
 }
 
@@ -64,12 +68,20 @@
 /** Details owner share: empty — sessionId arrives as a framework-standard prop. */
 export interface DetailsOwnerProps {}
 
+/** Aside owner share: live column state from the frame's concession solve. */
+export interface AsideOwnerProps {
+  /** True when the aside is closed (zero rendered width; the subtree stays mounted). */
+  collapsed: boolean
+  /** Rendered column width in px (0 when collapsed). */
+  width: number
+}
+
 /** Required services (cordis fiber inject — the loader passes the whole export surface as an object plugin). */
 export const inject = ['slots', 'theme']
 
 /**
  * Client plugin body: provide ctx.layout, then one register() call — AppFrame
- * into 'root' with the four child-slot declarations, the layout store seat,
+ * into 'root' with the five child-slot declarations, the layout store seat,
  * and the inject hook that hands the store's bound actions to the service.
  * @param ctx - client root context.
  */
@@ -83,6 +95,7 @@
         'sidebar': { kind: 'single', scope: 'root' },
         'conversation': { kind: 'single', scope: 'session-maybe' },
         'details': { kind: 'single', scope: 'session' },
+        'aside': { kind: 'single', scope: 'root' },
       },
       // Exclusive store: the factory itself — the framework instantiates per
       // entry and delivers useStore/actions to AppFrame as standard props.
diff -ru /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/service.ts /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/service.ts
--- /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/service.ts	2026-08-11 01:10:34
+++ /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/service.ts	2026-08-12 21:52:33
@@ -27,6 +27,10 @@
   openDetails(): void
   /** Close the details panel. */
   closeDetails(): void
+  /** Open the aside panel (no-op when already open). */
+  openAside(): void
+  /** Close the aside panel. */
+  closeAside(): void
 }
 
 /** Cross-plugin panel-action face (ctx.layout). */
@@ -57,6 +61,16 @@
   /** Close the details panel. */
   closeDetails(): void {
     this.#require().closeDetails()
+  }
+
+  /** Open the aside panel (no-op when already open). */
+  openAside(): void {
+    this.#require().openAside()
+  }
+
+  /** Close the aside panel. */
+  closeAside(): void {
+    this.#require().closeAside()
   }
 
   #require(): PanelActions {
diff -ru /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/stores.ts /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/stores.ts
--- /Users/white/.dsh/source/master/packages/client/ui-layout/src/client/stores.ts	2026-08-11 01:10:34
+++ /Users/white/.dsh/source/current/packages/client/ui-layout/src/client/stores.ts	2026-08-12 21:52:25
@@ -9,7 +9,8 @@
  */
 import { defineStore, type EngineStoreHandle } from '@deepseek-ai/dsh-client-runtime/client'
 import {
-  clampWidth, DETAILS_DEFAULT, DETAILS_MAX, DETAILS_MIN,
+  ASIDE_DEFAULT, ASIDE_MAX, ASIDE_MIN, clampWidth,
+  DETAILS_DEFAULT, DETAILS_MAX, DETAILS_MIN,
   SIDEBAR_DEFAULT, SIDEBAR_MAX, SIDEBAR_MIN,
 } from './columns.ts'
 
@@ -20,7 +21,7 @@
  * `narrowExpanded` is the manual override that re-expands the auto-collapsed
  * sidebar over the squeezed center without rewriting the width preference.
  */
-type LayoutState = { sidebar: number; details: number; narrow: boolean; narrowExpanded: boolean }
+type LayoutState = { sidebar: number; details: number; aside: number; narrow: boolean; narrowExpanded: boolean }
 
 /**
  * Annotation twin of the actions literal below (the export needs a declared
@@ -29,10 +30,13 @@
 type LayoutActions = {
   setSidebar: (draft: LayoutState, px: number) => void
   setDetails: (draft: LayoutState, px: number) => void
+  setAside: (draft: LayoutState, px: number) => void
   toggleSidebar: (draft: LayoutState) => void
   setNarrow: (draft: LayoutState, narrow: boolean) => void
   openDetails: (draft: LayoutState) => void
   closeDetails: (draft: LayoutState) => void
+  openAside: (draft: LayoutState) => void
+  closeAside: (draft: LayoutState) => void
 }
 
 /**
@@ -47,10 +51,11 @@
  */
 export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutActions>  {
   const handle = defineStore({
-    init: (): LayoutState => ({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false }),
+    init: (): LayoutState => ({ sidebar: SIDEBAR_DEFAULT, details: 0, aside: ASIDE_DEFAULT, narrow: false, narrowExpanded: false }),
     actions: {
       setSidebar: (d, px: number) => { d.sidebar = clampWidth(px, SIDEBAR_MIN, SIDEBAR_MAX) },
       setDetails: (d, px: number) => { d.details = clampWidth(px, DETAILS_MIN, DETAILS_MAX) },
+      setAside: (d, px: number) => { d.aside = clampWidth(px, ASIDE_MIN, ASIDE_MAX) },
       // Narrow toggles flip only the override: the width preference survives
       // untouched, so re-widening restores the pre-squeeze layout.
       toggleSidebar: (d) => {
@@ -66,6 +71,8 @@
       },
       openDetails: (d) => { if (d.details === 0) d.details = DETAILS_DEFAULT },
       closeDetails: (d) => { d.details = 0 },
+      openAside: (d) => { if (d.aside === 0) d.aside = ASIDE_DEFAULT },
+      closeAside: (d) => { d.aside = 0 },
     },
   })
   return handle
